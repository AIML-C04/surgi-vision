POSTGRES_USER=surgivision
POSTGRES_PASSWORD=password
POSTGRES_DB=surgivision

# Backend Settings
SECRET_KEY=yoursecretkey
DATABASE_URL=postgresql://surgivision:password@localhost:5432/surgivision

# Storage Configuration (local or supabase)
STORAGE_PROVIDER=local
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_BUCKET=videos

# AI Model Configuration (mock, local, real)
MODEL_PROVIDER=mock
MODEL_PATH=models/yolov8s_cholec80.pt
MODEL_CONFIDENCE_THRESHOLD=0.5

# RAG LLM Provider (mock, huggingface)
LLM_PROVIDER=mock
HF_TOKEN=
LLM_MODEL=mistralai/Mixtral-8x7B-Instruct-v0.1

# Frontend
VITE_API_URL=http://localhost:8000
